# Optional. CityCut runs with public endpoints and does not need an API key.
# Override these only if you host your own Overpass or Nominatim instance.
# VITE_OVERPASS_URL=https://overpass.openstreetmap.fr/api/interpreter
# VITE_NOMINATIM_URL=https://nominatim.openstreetmap.org
